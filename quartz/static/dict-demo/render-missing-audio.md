# Superseded

This file is superseded by `render_demo_audio.py` (full clip manifest, one worker run, ffmpeg conversion) and the repo-root `RENDER-AND-PUBLISH-DICT-DEMO.command` (render → commit → publish to main in one double-click).
